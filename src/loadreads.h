#ifndef LOADREADS_H
#define LOADREADS_H

// loadreads.h

#include <vector>
#include <string>

void load_data(std::vector<std::string>& data, std::string file);  // load all the data from file

#endif 
